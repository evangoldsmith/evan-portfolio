---
const { path, title, description, link } = Astro.props;


---
<a class="group flex md:flex-row flex-col border border-gray-500 rounded-md p-4 gap-6 items-center dark:hover:border-white hover:border-neutral-300 max-w-full " href={link}>
    <span class="rounded-md shadow-lg w-[100px] h-[100px] min-w-[100px] flex justify-center bg-slate-100">
      <img class="rounded-md shadow-lg"
          src={path.src}
          width="100"
          alt={title}>
    </span>
    <span class="flex items-center justify-between w-full gap-4">
        <div>
            <h1 class="text-2xl">{title}</h1>
            <p>{description}</p>
        </div>
        <div class="flex-1 flex justify-end">
            <i class="hidden group-hover:block fa-solid fa-arrow-up-right-from-square"></i>
        </div>
    </span>
</a>